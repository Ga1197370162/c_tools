CMAKE_MINIMUM_REQUIRED(VERSION 3.28)

PROJECT(c_tools_src LANGUAGES C CXX)

SET(SOURCE_PATH ${C_TOOLS_ROOT}/src CACHE PATH "c_tools source")
FILE(GLOB_RECURSE SOURCE_FILE ${SOURCE_PATH}/**.c)

ADD_LIBRARY(
  c_tools_static STATIC
  ${SOURCE_FILE}
)